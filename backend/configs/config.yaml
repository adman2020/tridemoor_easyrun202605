server:
  port: "8080"
database:
  dsn: "user:password@tcp(mysql:3306)/stridemoor?charset=utf8mb4&parseTime=True&loc=Asia%2FShanghai"
redis:
  addr: "redis:6379"
  password: ""
  db: 0
jwt:
  secret: "your_jwt_secret"
  expire_hours: 720
  refresh_days: 90
minio:
  endpoint: "minio:9000"
  access_key: "minio_access_key"
  secret_key: "minio_secret_key"
  bucket: "stridemoor"
  use_ssl: false
